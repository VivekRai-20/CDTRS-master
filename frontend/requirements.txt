# =====================================================================
# CDTRS (Centralised Document Tracking and Routing System)
# Strict Pinned Dependencies for Python 3.12.0 Environment
# PZ_26/08 - Exact versions tested for Python 3.12.0 (Zero C-API or version mismatches)
# =====================================================================

# ---------------------------------------------------------------------
# 1. Desktop Frontend GUI & Networking
# ---------------------------------------------------------------------
PySide6==6.7.2
requests==2.32.3
pip-system-certs==4.0
python-dotenv==1.0.1

# ---------------------------------------------------------------------
# 2. Data Science (Strict Python 3.12 C-API compatibility)
# ---------------------------------------------------------------------
numpy==1.26.4
pandas==2.2.2

# ---------------------------------------------------------------------
# 3. Backend Framework & Database
# ---------------------------------------------------------------------
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.8.2
pydantic-core==2.20.1
starlette==0.37.2
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
python-multipart==0.0.9
websockets==12.0

# ---------------------------------------------------------------------
# 4. Security & Cryptography
# ---------------------------------------------------------------------
bcrypt==4.0.1
passlib==1.7.4
python-jose==3.3.0
cryptography==42.0.8

# ---------------------------------------------------------------------
# 5. Document Processing & OCR Engine (Pure-Python + Neural)
# ---------------------------------------------------------------------
PyMuPDF==1.24.9
pypdf==4.3.1
opencv-python==4.10.0.84
Pillow==10.4.0
shapely==2.0.5
pyclipper==1.3.0.post5
scikit-image==0.24.0
# PaddlePaddle / PaddleOCR 3.x and the rest of the OCR engine's dependencies
# (layout, NER, semantic routing) are pinned by OCR_new itself:
-r ../OCR_new/requirements.txt

