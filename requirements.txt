# =====================================================================
# CDTRS (Centralised Document Tracking and Routing System)
# Strict Pinned Production Dependencies (== Versions)
# =====================================================================

# ---------------------------------------------------------------------
# 1. Desktop Frontend GUI (PySide6 / Qt6) & Build Tools
# ---------------------------------------------------------------------
setuptools==80.9.0
wheel==0.42.0
PySide6==6.7.2
requests==2.32.3
python-dotenv==1.0.1
pip-system-certs==4.0

# ---------------------------------------------------------------------
# 2. Backend Web Framework & Database
# ---------------------------------------------------------------------
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.8.2
pydantic-core==2.20.1
pydantic-settings==2.3.4
starlette==0.37.2
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
python-multipart==0.0.9
websockets==12.0
python-dateutil==2.9.0.post0

# ---------------------------------------------------------------------
# 3. Security, Authentication & Cryptography
# ---------------------------------------------------------------------
bcrypt==4.0.1
passlib==1.7.4
python-jose==3.3.0
cryptography==42.0.8

# ---------------------------------------------------------------------
# 4. Document Processing, Digital Extraction & OCR Engines
# ---------------------------------------------------------------------
PyMuPDF==1.24.9
pypdf==4.3.1
Pillow==10.4.0
pytesseract==0.3.13
pdf2image==1.17.0
opencv-python==4.10.0.84
numpy==1.26.4
pandas==2.2.2

# Neural OCR & Computer Vision (PaddleOCR v3.x Pipeline)
shapely==2.0.5
pyclipper==1.3.0.post5
scikit-image==0.24.0
# PaddlePaddle / PaddleOCR 3.x and the rest of the OCR engine's dependencies
# (layout, NER, semantic routing) are pinned by OCR_new itself:
-r OCR_new/requirements.txt


