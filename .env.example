# CDTRS keeps its settings in two files - there is no project-wide .env:
#
#   backend\.env    database, server host/port, security key, uploads, mail
#                   (template: backend\.env.example)
#   frontend\.env   address of the backend for the desktop application
#                   (template: frontend\.env.example)
#
# See readme.md, section "Configuration".
